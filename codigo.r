print("Olá, Mundo!")

soma <- 5 + 3
print(soma)

calcular_area_retangulo <- function(largura, altura) {
  area <- largura * altura
  return(area)
}

area_do_meu_retangulo <- calcular_area_retangulo(10, 5)
print(area_do_meu_retangulo)
